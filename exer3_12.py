# Cálculo do tempo de percurso

distancia = float(input("Digite a distância em km(s): "))
velocidade = float(input("Digite a velocidade média em km/h: "))
tempo = distancia / velocidade
print("O tempo médio da viagem será de :", tempo, "horas")
